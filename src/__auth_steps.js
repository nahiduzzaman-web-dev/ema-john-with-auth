/** 
 * 1. create a new firebase project in console.firebase.google.com 
 * 2. npm install firebase
 * 3. create firebase.init.js & import getAuth {import { getAuth } from 'firebase/auth'} to Export auth {const auth = getAuth(app);}
 * 4. settings > authentication > get startted > sign-in method
 * 5. create Login, Signup component, setup route
 * 6. attatch form field handler & form submit handler
 * 7. npm install --save react-firebase-hooks 
 * 8. useCreateUserWithEmailAndPassword from firebase hooks
 * 9. if user is created redirect to the expected 
 * 10. useSignInWithEmailAndPassword for sign in


*/





/**
 * Hosting Steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */ 